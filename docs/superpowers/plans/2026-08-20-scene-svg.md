# TV 背景换用 SVG 美术稿 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 TV 背景从程序化绘制的天际线换成 `public/scene/hobart.svg` 美术稿,同时保留全部动态特效——按真实日出日落变化的时间光效、飘云、水波、雨雪、雾、雷暴闪电、vignette 与整体压暗幕。

**Architecture:** SVG 内联进 DOM(`dangerouslySetInnerHTML`,React 视为单节点),构建期把各分组的字面色归并成**色槽**并改写成 `var(--sNN)`;运行时把现有 `scenePaint(t, fx, epoch)` 产出的 `ScenePaint` 映射到色槽,每档相位只更新约 40 个 CSS 变量。云/水波/倒影/星星走 CSS `transform`/`opacity`(合成器,不重绘);雨、雪、雾、闪电、vignette、压暗幕留在最上层一块 canvas。

**Tech Stack:** 与主项目一致(Next.js 15 / React 19 / Tailwind 3.4 / Vitest / Playwright),零新依赖。

---

## 执行约定

- **基线**:`main`(本计划文档提交时的 HEAD)。开工前 `git status` 应干净。
- **基线测试数(已实测 2026-08-20)**:`npx vitest run` → **28 files / 396 tests 全绿**(约 95s)。E2E 6 条约 3 分钟。
- **分支**:新建 `feature/scene-svg`。
- **每个 Task 收尾**:`npx tsc --noEmit` 零输出、全量 vitest 全绿、独立 commit。
- **先 Read 再写**:所有锚文本取自基线工作区;编辑前必须 Read 目标文件核对。
- 权威规格:`docs/superpowers/specs/2026-08-20-scene-svg-design.md`。

## 与规格的两处偏离(实施中发现,更优)

1. **色源用 `ScenePaint` 而不是 `Palette`。** 规格 §4.2 要给 `Palette` 加 `waterDeep`/`waterLight` 并手填 12 个数。实际 `hobart/paint.ts` 的 `FRAMES`(6 关键帧)产出的 `ScenePaint` 字段与 SVG 分组几乎一一对应,**新增手写颜色为 0**,且 §4.3 要求的三条天气行为(阴天压灰 / 云量抑制波光 / 灯光不压灰)已在 `scenePaint` 内实现并有测试。因此 `paint.ts` **不删除**,只搬出 `hobart/`。
2. **规格 §5 漏列四样现有特效**,本计划全部保留:雾幕(WMO 45/48)、雷暴闪电(含山脊高亮)、vignette、整体压暗幕 `dim`(0.35–0.45,前景文字对比度依赖它)。

## 文件结构

| 文件 | 职责 |
|---|---|
| `src/lib/scene/types.ts`(新) | `Rgb` / `ScenePaint` 类型 + `mulberry32` / `rgba` 工具。从 `hobart/geometry.ts` 抽出的、与画师无关的部分 |
| `src/lib/scene/paint.ts`(移动) | `scenePaint(t, fx, epoch) → ScenePaint`。由 `hobart/paint.ts` 搬来,去掉 `windowLitSchedule` |
| `src/lib/scene/windowLights.ts`(新) | `windowLitSchedule(now) → number`。纯时钟窗灯作息,行为一字不改 |
| `src/lib/scene/sceneSvg.ts`(构建产物) | 内联 SVG 字符串 + 色槽清单。由 `scripts/build-scene.ts` 生成,提交入库 |
| `src/lib/scene/slots.ts`(新) | `slotColors(sp) → Record<string,string>`:把 `ScenePaint` 映射成 CSS 变量值 |
| `scripts/build-scene.ts`(新) | 读 `public/scene/hobart.svg`,归并色槽,产出 `sceneSvg.ts` |
| `src/components/tv/SceneBackground.tsx`(新) | 装配器:SVG 层 + CSS 变量驱动 + canvas 特效层 |
| `src/lib/scene/hobart/`(删除) | 7 个画师 + `geometry.ts` |
| `src/components/tv/SkylineBackground.tsx`(删除) | 被 `SceneBackground` 取代 |

---

### Task 1: 把调色引擎搬出 hobart/(纯重构,零行为变化)

先把要留下的东西挪到安全位置,后续任务才能放心删 `hobart/`。本任务**不改任何行为**,现有测试断言一字不动,只改导入路径。

**Files:**
- Create: `src/lib/scene/types.ts`
- Create: `src/lib/scene/windowLights.ts`
- Create: `src/lib/scene/paint.ts`
- Modify: `src/lib/scene/hobart/geometry.ts`(改为从 `../types` re-export,画师不受影响)
- Modify: `src/lib/scene/hobart/paint.ts`(删除,内容已搬走)
- Modify: `src/components/tv/SkylineBackground.tsx`(导入路径)
- **不动**:7 个画师文件——它们只从 `./geometry` 导入,而 geometry 继续再导出这些名字
- Test: 拆 `tests/scene/hobart.test.ts` → `tests/scene/paint.test.ts` + `tests/scene/windowLights.test.ts`

- [ ] **Step 1:** Read `src/lib/scene/hobart/geometry.ts`,把 `Rgb` 类型、`ScenePaint` 类型、`mulberry32`、`rgba` 四项**逐字**复制到新建的 `src/lib/scene/types.ts`(含全部注释)。

- [ ] **Step 2:** 在 `src/lib/scene/hobart/geometry.ts` 顶部把这四项改成 re-export,body 里的原定义删除:

```ts
import type { Rgb, ScenePaint } from '../types';
export type { Rgb, ScenePaint };
export { mulberry32, rgba } from '../types';
```

**注意不能写成 `export type { Rgb, ScenePaint } from '../types';`** —— 那是纯再导出,不产生本地绑定,而 `geometry.ts` 里的 `LayerFn` / `WaterDynamicFn` / `CloudFn` 三个签名要在本地用 `ScenePaint`,会报 `Cannot find name 'ScenePaint'`。

跑 `npx tsc --noEmit`,零输出(画师与装配器的导入不变,仍从 geometry 拿)。

- [ ] **Step 3:** Read `src/lib/scene/hobart/paint.ts`。把 `windowLitSchedule` 及其上方两个常量 `DAY_BASE` / `NIGHT_PEAK` 和全部校准注释**逐字**搬进新建的 `src/lib/scene/windowLights.ts`;它用到的 `lerp` / `smoothstep` 一并搬(在 `paint.ts` 里找到定义后复制,不要重新实现)。

- [ ] **Step 4:** 把 `hobart/paint.ts` 剩余部分(`HFrame` / `FRAMES` / `STOPS` / `lerpFrame` / `frameAt` / `grayMix` / `scenePaint` / 三个 lamp 常量)**逐字**搬进新建的 `src/lib/scene/paint.ts`,导入改为:

```ts
import { getPalette } from '@/lib/scene/palette';
import type { SceneEffects } from '@/lib/scene/weather';
import type { Rgb, ScenePaint } from '@/lib/scene/types';
```

删除 `src/lib/scene/hobart/paint.ts`。

- [ ] **Step 5:** 把 `SkylineBackground.tsx` 第 5 行的导入改成:

```ts
import { scenePaint } from '@/lib/scene/paint';
import { windowLitSchedule } from '@/lib/scene/windowLights';
```

`npx tsc --noEmit` 零输出。

- [ ] **Step 6:** 拆测试。`tests/scene/hobart.test.ts` 里 `describe('scenePaint')` 整块移到新建的 `tests/scene/paint.test.ts`,`describe('windowLitSchedule ...')` 整块移到 `tests/scene/windowLights.test.ts`,**断言一字不改**,只改文件头的导入:

```ts
// tests/scene/paint.test.ts
import { describe, it, expect } from 'vitest';
import { scenePaint } from '@/lib/scene/paint';
import { effectsFromWeather } from '@/lib/scene/weather';

// tests/scene/windowLights.test.ts
import { describe, it, expect } from 'vitest';
import { windowLitSchedule } from '@/lib/scene/windowLights';
```

`describe('geometry contract')` 留在 `hobart.test.ts` 原地(Task 5 随画师一起删)。

- [ ] **Step 7:** `npx vitest run` —— 用例数不变,文件数因拆出两个而 +2:预期 **30 files / 396**。

- [ ] **Step 8:** Commit:

```bash
git add src/lib/scene tests/scene src/components/tv/SkylineBackground.tsx
git commit -m "refactor: lift the colour engine out of the hobart painters"
```

---

### Task 2: 构建脚本 —— 把 SVG 的字面色归并成色槽

**Files:**
- Create: `scripts/build-scene.ts`
- Create: `src/lib/scene/sceneSvg.ts`(脚本产物,提交入库)
- Modify: `package.json`(加 `build:scene` 脚本)
- Test: `tests/scene/scene-svg.test.ts`

**设计契约(后续任务依赖,签名不得改动):**

```ts
// src/lib/scene/sceneSvg.ts 的导出形状
export type SceneSlot = {
  id: string;      // 's0' | 's1' | …,即 CSS 变量名 --s0
  group: string;   // 'mountains' | 'city' | …,取自 <g id>
  lum: number;     // 0..1,该槽原始色的相对亮度(用来在色带上取位置)
};
export const SCENE_SLOTS: SceneSlot[] = [/* 生成 */];
export const SCENE_SVG: string = '<svg …>';  // fill/stroke/stop-color 已改成 var(--sN)
```

- [ ] **Step 1:** 写失败测试 `tests/scene/scene-svg.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import fs from 'node:fs';
import path from 'node:path';
import { SCENE_SLOTS, SCENE_SVG } from '@/lib/scene/sceneSvg';

const GROUPS = [
  'sky', 'sky-clouds', 'mountains', 'mountain-contours', 'mountain-contour-lines',
  'hillside-houses', 'water', 'water-sparkles', 'city', 'tasman-bridge',
  'reflections', 'sailboat', 'foreground-ripples',
];

describe('sceneSvg build output', () => {
  it('keeps every addressable group from the artwork', () => {
    for (const g of GROUPS) expect(SCENE_SVG).toContain(`<g id="${g}"`);
  });

  it('leaves no literal hex colour behind — every fill/stroke is a slot variable', () => {
    expect(SCENE_SVG).not.toMatch(/(fill|stroke|stop-color)="#[0-9A-Fa-f]{6}"/);
    expect(SCENE_SVG).toMatch(/fill="var\(--s\d+\)"/);
  });

  it('every slot referenced by the svg is declared, and every declared slot is used', () => {
    const used = new Set(Array.from(SCENE_SVG.matchAll(/var\(--(s\d+)\)/g), (m) => m[1]));
    const declared = new Set(SCENE_SLOTS.map((s) => s.id));
    expect([...used].filter((id) => !declared.has(id))).toEqual([]);
    expect([...declared].filter((id) => !used.has(id))).toEqual([]);
  });

  it('assigns every slot to a real group and a luminance in 0..1', () => {
    for (const s of SCENE_SLOTS) {
      expect(GROUPS).toContain(s.group);
      expect(s.lum).toBeGreaterThanOrEqual(0);
      expect(s.lum).toBeLessThanOrEqual(1);
    }
  });

  it('never merges colours from different groups into one slot', () => {
    const byId = new Map<string, string>();
    for (const s of SCENE_SLOTS) {
      expect(byId.get(s.id) ?? s.group).toBe(s.group);
      byId.set(s.id, s.group);
    }
    expect(byId.size).toBe(SCENE_SLOTS.length);
  });

  it('is in sync with public/scene/hobart.svg — the element count matches', () => {
    const src = fs.readFileSync(path.join(process.cwd(), 'public', 'scene', 'hobart.svg'), 'utf8');
    const count = (s: string) => (s.match(/<(rect|path|circle|line)\b/g) ?? []).length;
    expect(count(SCENE_SVG)).toBe(count(src));
  });
});
```

- [ ] **Step 2:** 跑 `npx vitest run tests/scene/scene-svg.test.ts` —— 预期全红,报 `Cannot find module '@/lib/scene/sceneSvg'`。

- [ ] **Step 3:** 写 `scripts/build-scene.ts`。核心算法:

```ts
import fs from 'node:fs';
import path from 'node:path';

const SRC = path.join(process.cwd(), 'public', 'scene', 'hobart.svg');
const OUT = path.join(process.cwd(), 'src', 'lib', 'scene', 'sceneSvg.ts');

/** <defs> 里的渐变按 id 前缀归组:mountainSoft* 归山体,waterGrad 归水,其余归天空。 */
function groupForGradient(id: string): string {
  if (id.startsWith('mountain')) return 'mountains';
  if (id.startsWith('water')) return 'water';
  return 'sky';
}

function luminance(hex: string): number {
  const [r, g, b] = [1, 3, 5].map((i) => parseInt(hex.slice(i, i + 2), 16));
  return (0.2126 * r + 0.7152 * g + 0.0722 * b) / 255;
}

/** 同组内亮度差 < TOL 的颜色归为同一个槽 —— 原画的明暗层次靠 lum 保留,不靠槽数。 */
const TOL = 0.04;

type Slot = { id: string; group: string; lum: number };

function build(): { svg: string; slots: Slot[] } {
  const src = fs.readFileSync(SRC, 'utf8');
  const slots: Slot[] = [];
  const byGroup = new Map<string, Slot[]>();

  const slotFor = (group: string, hex: string): string => {
    const lum = luminance(hex);
    const pool = byGroup.get(group) ?? [];
    const hit = pool.find((s) => Math.abs(s.lum - lum) < TOL);
    if (hit) return hit.id;
    const slot: Slot = { id: `s${slots.length}`, group, lum };
    slots.push(slot);
    pool.push(slot);
    byGroup.set(group, pool);
    return slot.id;
  };

  // 逐行扫描:<g id> 入栈、</g> 出栈;<linearGradient id> 切换 defs 内的归属。
  const stack: string[] = [];
  let group = 'sky';        // <defs> 之前的元素(无)与兜底
  let gradGroup: string | null = null;

  const out = src.split('\n').map((line) => {
    const gm = /<g id="([^"]+)"/.exec(line);
    if (gm) { stack.push(group); group = gm[1]; }
    const dm = /<(?:linear|radial)Gradient id="([^"]+)"/.exec(line);
    if (dm) gradGroup = groupForGradient(dm[1]);
    if (line.includes('</linearGradient>') || line.includes('</radialGradient>')) gradGroup = null;

    const owner = gradGroup ?? group;
    const rewritten = line.replace(
      /(fill|stroke|stop-color)="(#[0-9A-Fa-f]{6})"/g,
      (_m, attr: string, hex: string) => `${attr}="var(--${slotFor(owner, hex.toUpperCase())})"`,
    );
    if (line.includes('</g>') && stack.length) group = stack.pop()!;
    return rewritten;
  }).join('\n');

  return { svg: out, slots };
}

const { svg, slots } = build();
fs.writeFileSync(OUT,
  `// 构建产物 —— 由 scripts/build-scene.ts 从 public/scene/hobart.svg 生成,请勿手改。\n`
  + `// 重新生成:npm run build:scene\n\n`
  + `export type SceneSlot = { id: string; group: string; lum: number };\n\n`
  + `export const SCENE_SLOTS: SceneSlot[] = ${JSON.stringify(slots, null, 2)};\n\n`
  + `export const SCENE_SVG = ${JSON.stringify(svg)};\n`,
  'utf8');
console.log(`[build-scene] ${slots.length} slots across ${new Set(slots.map((s) => s.group)).size} groups`);
```

- [ ] **Step 4:** `package.json` 的 `scripts` 里,在 `"db:migrate"` 之后加一行:

```json
    "build:scene": "tsx scripts/build-scene.ts",
```

- [ ] **Step 5:** 跑 `npm run build:scene`。预期输出形如 `[build-scene] NN slots across 13 groups`,NN 在 25–60 之间。若超过 60,把 `TOL` 从 0.04 调到 0.06 重跑——槽越少运行时越省,但同组内亮度层次不能被压平(下一步的测试会验)。

- [ ] **Step 6:** 跑 `npx vitest run tests/scene/scene-svg.test.ts` —— 预期 6 条全绿。

- [ ] **Step 7:** `npx tsc --noEmit` 零输出;`npx vitest run` 全量 **31 files / 402**(新增 6 条)。

- [ ] **Step 8:** Commit:

```bash
git add scripts/build-scene.ts src/lib/scene/sceneSvg.ts tests/scene/scene-svg.test.ts package.json
git commit -m "feat: build the scene svg into colour slots"
```

---

### Task 3: 色槽取色 —— ScenePaint → CSS 变量

**Files:**
- Create: `src/lib/scene/slots.ts`
- Test: `tests/scene/slots.test.ts`

**设计契约:**

```ts
export function slotColors(sp: ScenePaint): Record<string, string>;
// 返回 { '--s0': '#1A2B3C', … },键含 `--` 前缀,可直接喂 style.setProperty
```

每个分组一条**色带**(暗端 → 亮端),槽位在色带上取值。色带全部取自 `ScenePaint`,不新增颜色。

**取值必须在组内归一化**,不能直接拿绝对亮度当插值参数:

```
t = (slot.lum - 该组最小 lum) / (该组最大 lum - 该组最小 lum)
```

理由:色带两端是任意两个语义色,它们的亮度并不对应 0 和 1。直接用绝对亮度会把整组挤在色带的一小段里——实测 `sailboat` 的三个槽会全落在亮度 0.20–0.23(白帆和船体同色,帆船变成一坨黑影),`city` 的七个槽全落在 0.19–0.29(白雨棚与桅杆线融进楼体,码头细节整片消失)。归一化后每组最暗的槽必落在色带暗端、最亮的必落在亮端,原画的**相对**明暗关系才映射得上去。

组内 lum 上下界从 `SCENE_SLOTS` 算一次即可(模块加载时算好,不必每次调用重算)。单槽组(`hillside-houses` / `mountain-contours`)分母为 0,取 `t = 0.5`。

| 分组 | 暗端 → 亮端 | 备注 |
|---|---|---|
| `sky` | `sky.top` → `sky.horizon` | |
| `sky-clouds` | `sky.cloudShade` → `sky.cloud` | |
| `mountains` / `mountain-contours` | `far.ridgeNear` → `far.ridgeFar` | 远脊更浅 |
| `mountain-contour-lines` | `far.ridgeNear` → `far.mist` | 等高线最浅 |
| `hillside-houses` | `light.window` → `light.window` | 恒暖色,亮度由整组 opacity 控 |
| `city` | `near.silhouette` → `far.mist` | 亮端是码头的白色雨棚与桅杆线,必须够浅 |
| `tasman-bridge` | `mid.bridge` → `far.mist` | 亮端是桥墩高光与桥面亮线 |
| `water` | `water.base` → `water.ripple` | |
| `water-sparkles` | `water.base` → `water.glitter` | 暗端要能融进水面,否则碎光整片过曝 |
| `reflections` | `water.reflection` → `light.waterGlow` | |
| `sailboat` | `water.hull` → `sky.cloud` | 亮端是白帆——和云同为受光的白布 |
| `foreground-ripples` | `water.base` → `water.ripple` | |

- [ ] **Step 1:** 写失败测试 `tests/scene/slots.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { slotColors } from '@/lib/scene/slots';
import { scenePaint } from '@/lib/scene/paint';
import { effectsFromWeather } from '@/lib/scene/weather';
import { SCENE_SLOTS } from '@/lib/scene/sceneSvg';

const CLEAR = effectsFromWeather(0, 0);       // 晴,云量 0.1
const OVERCAST = effectsFromWeather(3, 0);    // 阴,云量 0.9

/** '#1A2B3C' → 相对亮度 0..1 */
function lum(hex: string): number {
  const [r, g, b] = [1, 3, 5].map((i) => parseInt(hex.slice(i, i + 2), 16));
  return (0.2126 * r + 0.7152 * g + 0.0722 * b) / 255;
}
const groupSlots = (g: string) => SCENE_SLOTS.filter((s) => s.group === g);

describe('slotColors', () => {
  it('returns a colour for every declared slot, keyed with the -- prefix', () => {
    const out = slotColors(scenePaint(0.5, CLEAR));
    expect(Object.keys(out)).toHaveLength(SCENE_SLOTS.length);
    for (const s of SCENE_SLOTS) expect(out[`--${s.id}`]).toMatch(/^#[0-9A-F]{6}$/);
  });

  it('preserves the artwork ordering inside a group — brighter source stays brighter', () => {
    const out = slotColors(scenePaint(0.5, CLEAR));
    for (const g of ['mountains', 'city', 'water']) {
      const ranked = [...groupSlots(g)].sort((a, b) => a.lum - b.lum);
      const got = ranked.map((s) => lum(out[`--${s.id}`]));
      for (let i = 1; i < got.length; i++) expect(got[i]).toBeGreaterThanOrEqual(got[i - 1] - 1e-6);
    }
  });

  it('night is darker than midday for every scenery group', () => {
    const day = slotColors(scenePaint(0.5, CLEAR));
    const night = slotColors(scenePaint(1, CLEAR));
    for (const g of ['sky', 'mountains', 'city', 'water']) {
      for (const s of groupSlots(g)) {
        expect(lum(night[`--${s.id}`])).toBeLessThan(lum(day[`--${s.id}`]));
      }
    }
  });

  it('overcast desaturates the scenery (设计 §4.3 阴天压灰)', () => {
    const clear = slotColors(scenePaint(0.5, CLEAR));
    const dull = slotColors(scenePaint(0.5, OVERCAST));
    const spread = (hex: string) => {
      const ch = [1, 3, 5].map((i) => parseInt(hex.slice(i, i + 2), 16));
      return Math.max(...ch) - Math.min(...ch);
    };
    const id = `--${groupSlots('sky')[0].id}`;
    expect(spread(dull[id])).toBeLessThan(spread(clear[id]));
  });

  it('lamp colours are never desaturated by weather (设计 §4.3 灯光保暖色)', () => {
    const clear = slotColors(scenePaint(1, CLEAR));
    const dull = slotColors(scenePaint(1, OVERCAST));
    for (const s of groupSlots('hillside-houses')) {
      expect(dull[`--${s.id}`]).toBe(clear[`--${s.id}`]);
    }
  });

  it('never flattens a group — midday keeps visible light and shade inside each group', () => {
    // 归一化前实测:sailboat 三个槽全在 0.20–0.23(白帆与船体同色)、city 七个槽全在
    // 0.19–0.29(白雨棚融进楼体)。这条把那种坍塌钉死。
    const out = slotColors(scenePaint(0.5, CLEAR));
    for (const g of [...new Set(SCENE_SLOTS.map((s) => s.group))]) {
      const got = groupSlots(g).map((s) => lum(out[`--${s.id}`]));
      if (got.length < 2) continue;  // 单槽组无所谓层次
      expect(Math.max(...got) - Math.min(...got)).toBeGreaterThanOrEqual(0.08);
    }
  });

  it('is pure — same input twice gives the identical object', () => {
    const sp = scenePaint(0.68, CLEAR);
    expect(slotColors(sp)).toEqual(slotColors(sp));
  });
});
```

- [ ] **Step 2:** 跑 `npx vitest run tests/scene/slots.test.ts` —— 预期全红,`Cannot find module '@/lib/scene/slots'`。

- [ ] **Step 3:** 写 `src/lib/scene/slots.ts`:

```ts
// 色槽取色:把 scenePaint 产出的 ScenePaint 铺到 sceneSvg 的色槽上(SVG 场景设计 §4.2)。
// 每组一条暗→亮色带,槽位按构建期记下的原始亮度在带上取值 —— 原画的明暗层次因此保留。
// 纯函数,零 DOM。

import { SCENE_SLOTS } from './sceneSvg';
import type { Rgb, ScenePaint } from './types';

type Ramp = [Rgb, Rgb];

function ramps(sp: ScenePaint): Record<string, Ramp> {
  return {
    'sky': [sp.sky.top, sp.sky.horizon],
    'sky-clouds': [sp.sky.cloudShade, sp.sky.cloud],
    'mountains': [sp.far.ridgeNear, sp.far.ridgeFar],
    'mountain-contours': [sp.far.ridgeNear, sp.far.ridgeFar],
    'mountain-contour-lines': [sp.far.ridgeFar, sp.far.mist],
    'hillside-houses': [sp.light.window, sp.light.window],
    'city': [sp.near.silhouette, sp.mid.silhouette],
    'tasman-bridge': [sp.mid.bridge, sp.far.ridgeNear],
    'water': [sp.water.base, sp.water.ripple],
    'water-sparkles': [sp.water.ripple, sp.water.glitter],
    'reflections': [sp.water.reflection, sp.light.waterGlow],
    'sailboat': [sp.water.hull, sp.near.wharf],
    'foreground-ripples': [sp.water.base, sp.water.ripple],
  };
}

function hex(c: Rgb): string {
  return '#' + c.map((v) => Math.max(0, Math.min(255, Math.round(v)))
    .toString(16).padStart(2, '0').toUpperCase()).join('');
}

/** 各组的 lum 上下界:模块加载时算一次,用于把槽位归一化到自己组的色带上。 */
const BOUNDS: Record<string, [number, number]> = (() => {
  const b: Record<string, [number, number]> = {};
  for (const s of SCENE_SLOTS) {
    const cur = b[s.group] ?? [1, 0];
    b[s.group] = [Math.min(cur[0], s.lum), Math.max(cur[1], s.lum)];
  }
  return b;
})();

export function slotColors(sp: ScenePaint): Record<string, string> {
  const table = ramps(sp);
  const out: Record<string, string> = {};
  for (const slot of SCENE_SLOTS) {
    const [dark, light] = table[slot.group] ?? table['mountains'];
    const [lo, hi] = BOUNDS[slot.group];
    const t = hi > lo ? (slot.lum - lo) / (hi - lo) : 0.5;  // 单槽组取中点
    out[`--${slot.id}`] = hex([
      dark[0] + (light[0] - dark[0]) * t,
      dark[1] + (light[1] - dark[1]) * t,
      dark[2] + (light[2] - dark[2]) * t,
    ]);
  }
  return out;
}
```

- [ ] **Step 4:** 跑 `npx vitest run tests/scene/slots.test.ts` —— 预期 6 条全绿。若「保序」那条红,说明某组的色带暗端比亮端还亮,把该组的两端对调。

- [ ] **Step 5:** `npx tsc --noEmit` 零输出;`npx vitest run` 全量 **32 files / 408**(新增 6 条)。

- [ ] **Step 6:** Commit:

```bash
git add src/lib/scene/slots.ts tests/scene/slots.test.ts
git commit -m "feat: map ScenePaint onto the svg colour slots"
```

---

### Task 4: SVG 背景层 —— 上色 + 动起来

本任务只做 SVG 层;雨雪雾闪电等 canvas 特效在 Task 5。做完 TV 上应当能看到完整的静态场景 + 飘云 + 水动,颜色随时间变化。

**Files:**
- Create: `src/components/tv/SceneBackground.tsx`
- Modify: `src/app/globals.css`(场景动画 keyframes)
- Modify: `src/components/tv/TvApp.tsx:369,379`(换组件)

- [ ] **Step 1:** 在 `src/app/globals.css` 末尾追加动画定义。**只动 `transform` 与 `opacity`**,保证走合成器:

```css
/* —— SVG 场景动态层(SVG 场景设计 §5)——
   全部只改 transform/opacity,不触发重绘;paused 时整棵树 animation-play-state: paused。 */
@keyframes scene-drift { from { transform: translateX(0); } to { transform: translateX(2196px); } }
@keyframes scene-breathe { 0%, 100% { opacity: .55; } 50% { opacity: 1; } }
@keyframes scene-swell { 0%, 100% { transform: translateY(0) scaleY(1); }
                          50% { transform: translateY(2px) scaleY(1.06); } }
@keyframes scene-twinkle { 0%, 100% { opacity: .35; } 50% { opacity: 1; } }

.scene-root.is-paused * { animation-play-state: paused !important; }

/* 云:每朵一个包裹 <g>,速度由内联 animation-duration 给,循环横穿两倍画幅后回绕 */
.scene-cloud { animation: scene-drift linear infinite; }
/* 水面碎光:构建期分成 4 个子层,错开相位呼吸 + 慢速横移 */
.scene-sparkle { animation: scene-breathe ease-in-out infinite, scene-drift linear infinite; }
.scene-ripple  { animation: scene-drift linear infinite; }
.scene-reflect { animation: scene-swell ease-in-out infinite; }
.scene-star    { animation: scene-twinkle ease-in-out infinite; }
```

- [ ] **Step 2:** 扩展 `scripts/build-scene.ts`,给动态分组加类名、给灯位加阈值。

先在文件顶部补一行导入(下面的 `lampRand` 要用):

```ts
import { mulberry32 } from '../src/lib/scene/types';
```

改动**在 `build()` 的逐行扫描里做**——事后用正则去猜元素位置是不可靠的。把 Task 2 里 `out = src.split('
').map(...)` 的回调改成下面这样(新增的是 `decorate` 一段):

```ts
  let cloudIdx = 0;
  // 灯位阈值用固定种子,保证每次构建产物一致(mulberry32,与场景其余随机同源)。
  const lampRand = mulberry32(4242);
  const out = src.split('
').map((line) => {
    const gm = /<g id="([^"]+)"/.exec(line);
    if (gm) { stack.push(group); group = gm[1]; }
    const dm = /<(?:linear|radial)Gradient id="([^"]+)"/.exec(line);
    if (dm) gradGroup = groupForGradient(dm[1]);
    if (line.includes('</linearGradient>') || line.includes('</radialGradient>')) gradGroup = null;

    const owner = gradGroup ?? group;
    let rewritten = line.replace(
      /(fill|stroke|stop-color)="(#[0-9A-Fa-f]{6})"/g,
      (_m, attr: string, hex: string) => `${attr}="var(--${slotFor(owner, hex.toUpperCase())})"`,
    );

    // 动态分组:开组标签加类名;云是逐朵包裹,所以在组内按行处理。
    if (gm) {
      const cls: Record<string, string> = {  // eslint-disable-line
        'water-sparkles': 'scene-sparkle" style="animation-duration:7s,220s',
        'foreground-ripples': 'scene-ripple" style="animation-duration:180s',
        'reflections': 'scene-reflect" style="animation-duration:9s',
        'stars': 'scene-stars',
      };
      const c = cls[gm[1]];
      if (c) rewritten = rewritten.replace(`<g id="${gm[1]}"`, `<g id="${gm[1]}" class="${c}"`);
    } else if (group === 'hillside-houses' && line.trimStart().startsWith('<rect')) {
      // 灯位:每个元素带自己的阈值,运行时按 windowLit 逐个开关(见下方"灯光机制")。
      rewritten = rewritten.replace('<rect ', `<rect class="scene-lamp" data-t="${lampRand().toFixed(4)}" `);
    } else if (group === 'city' && /fill="var\(--s\d+\)"/.test(line) && line.includes('rx="0.5"')) {
      rewritten = rewritten.replace('<rect ', `<rect class="scene-lamp" data-t="${lampRand().toFixed(4)}" `);
    } else if (group === 'sky-clouds' && line.trimStart().startsWith('<path')) {
      // 每朵云单独包一层:速度 120/165/210/255s 循环,负延迟错开起始位置。
      const dur = 120 + (cloudIdx % 4) * 45;
      rewritten = `<g class="scene-cloud" data-i="${cloudIdx}" `
        + `style="animation-duration:${dur}s;animation-delay:-${cloudIdx * 17}s">${rewritten}</g>`;
      cloudIdx += 1;
    }

    if (line.includes('</g>') && stack.length) group = stack.pop()!;
    return rewritten;
  }).join('
');
```

同时把根标签改写为铺满裁切(规格 §6),放在 `build()` 的 `return` 之前:

```ts
  const svg = out.replace(
    '<svg xmlns="http://www.w3.org/2000/svg" width="1832" height="859" viewBox="0 0 1832 859" fill="none">',
    '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1832 859" fill="none" '
    + 'preserveAspectRatio="xMidYMid slice" style="width:100%;height:100%;display:block">');
  return { svg, slots };
```

- [ ] **Step 3:** 重跑 `npm run build:scene`,确认 `SCENE_SVG` 里出现 8 处 `class="scene-cloud"`。跑 `npx vitest run tests/scene/scene-svg.test.ts` —— 元素计数那条会因包裹 `<g>` 而仍然通过(`<g>` 不在计数正则里)。

- [ ] **Step 4:** 写 `src/components/tv/SceneBackground.tsx`:

```tsx
'use client';

import { useEffect, useRef } from 'react';
import { phaseFromClock } from '@/lib/scene/palette';
import { effectsFromWeather } from '@/lib/scene/weather';
import { scenePaint } from '@/lib/scene/paint';
import { windowLitSchedule } from '@/lib/scene/windowLights';
import { slotColors } from '@/lib/scene/slots';
import { SCENE_SVG } from '@/lib/scene/sceneSvg';
import type { TvWeather } from '@/lib/types';

const CACHE_T_STEP = 0.015;    // 沿用现有阈值
const WINDOW_LIT_STEP = 0.02;
const TICK_MS = 1000;          // 每秒查一次是否跨档,跨了才写变量

export default function SceneBackground({
  weather,
  paused,
}: {
  weather: TvWeather | null;
  paused: boolean;
}) {
  const rootRef = useRef<HTMLDivElement | null>(null);
  const weatherRef = useRef<TvWeather | null>(weather);
  useEffect(() => { weatherRef.current = weather; }, [weather]);

  useEffect(() => {
    const root = rootRef.current;
    if (!root) return;
    let key = '';
    const apply = () => {
      const w = weatherRef.current;
      const fx = effectsFromWeather(w?.weatherCode ?? 0, w?.windSpeedKmh ?? 0);
      const now = new Date();
      const t = phaseFromClock(now, w?.sunrise, w?.sunset);
      const lit = windowLitSchedule(now);
      const next = `${Math.round(t / CACHE_T_STEP)}|${Math.round(lit / WINDOW_LIT_STEP)}`
        + `|${Math.round(fx.cloudiness * 10)}`;
      if (next === key) return;
      key = next;
      const sp = scenePaint(t, fx);
      sp.light.windowLit = lit;
      for (const [name, value] of Object.entries(slotColors(sp))) {
        root.style.setProperty(name, value);
      }
      // 灯光整体亮度:山坡房屋与城市窗户共用窗灯作息
      root.style.setProperty('--lit', String(lit));
      // 云量决定显示几朵云(2–8)
      root.style.setProperty('--cloud-count', String(2 + Math.round(fx.cloudiness * 6)));
      root.style.setProperty('--star', String(sp.sky.star * (1 - fx.cloudiness * 0.85)));
    };
    apply();
    const id = window.setInterval(apply, TICK_MS);
    return () => window.clearInterval(id);
  }, []);

  return (
    <div
      ref={rootRef}
      aria-hidden="true"
      className={`scene-root pointer-events-none fixed inset-0 z-0 h-full w-full${paused ? ' is-paused' : ''}`}
      dangerouslySetInnerHTML={{ __html: SCENE_SVG }}
    />
  );
}
```

- [ ] **Step 5:** 灯光与星空的联动。

**星空**走纯 CSS 变量:

```css
#stars { opacity: var(--star, 0); }
```

**灯光不能用整组透明度。** `windowLitSchedule` 返回的是「每盏灯独立点亮的概率」——白天 `DAY_BASE = 0.0075`,夜里峰值 0.92。当成整组 opacity 用的话,白天 0.0075 等于整组隐形,原设计「白天零星两三盏」这个细节就没了;夜里也会变成所有灯一起半透,而不是「大部分亮、少数暗」的质感。

所以保持**逐元素伯努利判定**:构建期已给每个灯位写了 `data-t`(固定种子的均匀随机阈值),运行时逐个开关。在 `apply()` 末尾加:

```ts
      root.querySelectorAll<HTMLElement>('.scene-lamp').forEach((el) => {
        el.style.display = Number(el.dataset.t) < lit ? '' : 'none';
      });
```

灯位共 254 个(山坡房屋 172 + 城市窗户 82)。`DAY_BASE = 0.0075` 在这个基数下期望点亮 **1.9 盏**,原 hobart 版按加权候选 ≈340.8 算是 2.6 盏——两者都落在设计要的「大约两三盏」里,**常数原样沿用,不需要重新校准**。

只有跨档时才会执行这段(`apply()` 自带 key 判重),不是每帧。

云的**显示朵数**做不到用 CSS 变量驱动 `nth-child`,在 `SceneBackground` 的 `apply()` 末尾用 JS 直接控制(构建期已给每朵云加了 `data-i`):

```ts
      const count = 2 + Math.round(fx.cloudiness * 6);
      root.querySelectorAll<HTMLElement>('.scene-cloud').forEach((el, i) => {
        el.style.display = i < count ? '' : 'none';
      });
```

- [ ] **Step 6:** 把 `TvApp.tsx` 两处 `SkylineBackground` 换成 `SceneBackground`(第 10 行导入、第 369/379 行用法),props 不变。

- [ ] **Step 7:** `npx tsc --noEmit` 零输出;`npx vitest run` 全量绿。

- [ ] **Step 7b:** **查山体渐变的归属**。`<defs>` 里的 `mountainSoftLight` / `mountainSoftShade` / `mountainSoftShade2` 三个渐变被 `groupForGradient` 归到了 `mountains` 组,但使用它们的路径在 `mountain-contours` 组里——两者走不同色带,半透明叠加后会在山体上留下颜色偏离的斑(2026-08-20 用修正色带渲染 MIDDAY/NIGHT 时目视到)。截图确认这个斑是否碍眼;碍眼就把 `groupForGradient` 里 `id.startsWith('mountain')` 的返回值改成 `'mountain-contours'`,重跑 `npm run build:scene` 再看。改之前先截图存档,好做前后对比。

- [ ] **Step 8:** **截图目验(用户门禁)**。用 `e2e/start-server.ts` 起服务,Playwright 以 1920×1080 打开 `/tv`,截 6 张:把系统时钟分别设到六个关键帧对应时刻(或临时给 `SceneBackground` 加一个 `?t=` 查询参数覆盖相位,验完删)。输出到 `<scratchpad>/scene-shots/`。**Read 每一张自查**,再交需求方确认 16:9 裁切构图(规格 §6 明确要求这一步)。

- [ ] **Step 9:** Commit:

```bash
git add src/components/tv/SceneBackground.tsx src/components/tv/TvApp.tsx src/app/globals.css scripts/build-scene.ts src/lib/scene/sceneSvg.ts
git commit -m "feat: render the svg scene with slot colouring and drifting layers"
```

---

### Task 5: canvas 特效层 + 删除旧实现

**Files:**
- Modify: `src/components/tv/SceneBackground.tsx`(加 canvas 层)
- Delete: `src/lib/scene/hobart/`(7 个画师 + `geometry.ts`)
- Delete: `src/components/tv/SkylineBackground.tsx`
- Modify: `tests/scene/hobart.test.ts` → 只留 `mulberry32` 那条,改名 `tests/scene/types.test.ts`

保留的六样特效,全部从 `SkylineBackground.tsx` **逐字搬运**,不重写:

| 特效 | 来源(基线行号) | 搬运后的位置 |
|---|---|---|
| 雨 | 第 8 段 `if (fx.rain > 0)` | canvas |
| 雪 | 第 9 段 `if (fx.snow > 0)` | canvas |
| 雾幕 | 第 7 段 `if (fx.fog)` | canvas |
| 雷暴闪电 | 第 10 段 `if (fx.thunder)` | canvas,**山脊高亮改为**给 `#mountains` 临时加 `.scene-flash` 类(原实现依赖被删的 `MOUNTAIN.farRidge`) |
| vignette | 第 11 段 | canvas |
| 整体压暗幕 `dim` | 第 12 段 | canvas |

- [ ] **Step 1:** 在 `SceneBackground` 里加一块 `<canvas className="pointer-events-none fixed inset-0 z-0 h-full w-full">`,置于 SVG `<div>` 之后(同 z-0,后者在上)。rAF 循环沿用基线的 `MAX_DPR = 1.5` / `FRAME_MIN_MS = 30` / `MAX_RAIN = 300` / `MAX_SNOW = 150` 与 `buildScene` 里的 `rain`/`snow` 粒子构造(星星与云不再需要,从 `Scene` 类型里删掉)。

- [ ] **Step 2:** 逐字搬运上表六段绘制代码。闪电的山脊高亮替换为:

```ts
// 原实现用 MOUNTAIN.farRidge 描边山脊;SVG 版直接给山体分组加一帧高亮类。
const mountains = rootRef.current?.querySelector('#mountains');
mountains?.classList.add('scene-flash');
window.setTimeout(() => mountains?.classList.remove('scene-flash'), 90);
```

  并在 `globals.css` 加:

```css
.scene-flash { filter: brightness(1.6); }
```

- [ ] **Step 3:** `paused` 时停 rAF(沿用基线 `if (paused) return;` 的 effect 结构),并给 SVG 根加 `is-paused`(Task 4 已实现)。

- [ ] **Step 4:** 删除 `src/lib/scene/hobart/` 整个目录与 `src/components/tv/SkylineBackground.tsx`。`npx tsc --noEmit` 必须零输出——若报错说明还有文件在引用画师,逐个改到新位置。

- [ ] **Step 5:** 处理 `tests/scene/hobart.test.ts`:删除 `describe('geometry contract')` 里 bands 与 anchors 两条(几何契约随画师消失),保留 `mulberry32 is deterministic per seed` 一条,文件改名 `tests/scene/types.test.ts`,导入改 `@/lib/scene/types`。

- [ ] **Step 6:** `npx vitest run` 全量绿(文件数与用例数以实际为准,只需确认**无红**且 `windowLitSchedule` 的 10 条、`scenePaint` 的 10 条仍在)。

- [ ] **Step 7:** Commit:

```bash
git add -A
git commit -m "feat: move weather effects onto the scene canvas and drop the painters"
```

---

### Task 6: 回归、目验与文档

- [ ] **Step 1:** `npx tsc --noEmit` 零输出;`npx vitest run` 全量全绿。

- [ ] **Step 2:** 清 `.next` 后 `npm run build`(共享目录竞态败则重试,至多再试两次)。

- [ ] **Step 3:** `npm run test:e2e` —— **6 passed**。E2E 不加新用例(规格 §8)。

- [ ] **Step 4:** **截图目验**:6 个关键帧 × 晴/雨/雪 共 18 张,输出到 `<scratchpad>/scene-shots/`。Read 全部自查:夜里山坡有灯火、城市窗户点亮、月亮在天上;雨雪粒子可见;阴天整体压灰但灯光仍暖。

- [ ] **Step 5:** **性能验证(规格 §7)**:在目标电视上打开 `/tv`,用 DevTools Performance 录 10 秒,确认稳定 30fps 以上、无长任务。若掉帧,按规格 §7 的退路执行——把 `#mountains` / `#city` / `#tasman-bridge` / `#sailboat` 四个静态分组在色相跨档时预光栅化成离屏位图,只保留动态分组在 DOM。

- [ ] **Step 6:** README 的 Architecture 一节把「天际线程序化绘制」相关描述改为 SVG 场景,并补一句:改动美术稿后要跑 `npm run build:scene` 重新生成 `sceneSvg.ts`。

- [ ] **Step 7:** Commit:

```bash
git add -A
git commit -m "docs: describe the svg scene pipeline"
```

---

## Self-Review(计划完成后自查,已执行)

1. **规格逐节覆盖**:§2 素材 → 已入库,Task 2 消费;§3 架构 → Task 1(搬引擎)+ Task 4(SVG 层)+ Task 5(canvas 层与删除);§4 上色 → Task 2(构建期色槽)+ Task 3(运行时取色),§4.3 三条天气行为由 `scenePaint` 原样承担并在 Task 3 各有测试,§4.4 阈值在 Task 4 Step 4;§5 动态层 → Task 4(云/碎光/波纹/倒影/星)+ Task 5(雨/雪/日月已在 SVG 或 canvas);§6 画幅 → Task 4 Step 2(preserveAspectRatio)与 Step 8 的用户门禁;§7 性能与退路 → Task 6 Step 5;§8 测试 → 各任务内联 + Task 6;§9 非目标未引入;§10 成功标准 → Task 6 全部门禁。
2. **规格漏项已补**:雾、闪电、vignette、压暗幕四样在 Task 5 有逐条搬运表。
3. **占位符扫描**:初稿的 Task 4 Step 2 / Step 6 曾以「先给脆弱写法再纠正」的形式呈现——实施者若跳读会照抄错的那份,已改为只给最终写法(逐行扫描包裹云、JS 控云朵数),并把重复的根标签改写合并进 Step 2。其余步骤均为可直接执行的完整代码或命令,无 TBD。
4. **类型一致性**:`SceneSlot { id, group, lum }` 在 Task 2 定义,Task 3 的 `slotColors` 与测试逐字沿用;`slotColors` 返回键带 `--` 前缀这一点在契约、实现、测试、组件四处一致;`ScenePaint` 字段名全部取自基线 `geometry.ts` 的类型定义,未杜撰。
